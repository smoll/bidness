#!/bin/sh

# script/server: Launch the application and any extra required processes
#                locally.

set -e

cd "$(dirname "$0")/.."

script/bootstrap

test -z "$RAILS_ENV" &&
  RAILS_ENV='development'

foreman start
